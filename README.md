# Intelligent Web Scraper and Crawler #

Scrape can be used to scrape both static and dynamic websites. Scrape can run parallel processes asynchronously. Scrape runs in JavaScript and on top of selenium. 